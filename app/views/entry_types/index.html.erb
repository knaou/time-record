<p id="notice"><%= notice %></p>

<h1>Listing Types</h1>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Position</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @entry_types.each do |type| %>
      <tr>
        <td><%= type.name %></td>
        <td><%= type.position %></td>
        <td><%= link_to 'Show', type %></td>
        <td><%= link_to 'Edit', edit_entry_type_path(type) %></td>
        <td><%= link_to 'Destroy', type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Type', new_entry_type_path %>
